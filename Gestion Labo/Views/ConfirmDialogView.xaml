<Window x:Class="Gestion_Labo.Views.ConfirmDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gestion_Labo.Views"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="ConfirmDialogView" Background="LightGray" Height="200" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="None">
    <Grid>
        <StackPanel>

            <DockPanel>
                <Button HorizontalAlignment="Right" x:Name="CloseDialog" Background="{x:Null}" BorderBrush="{x:Null}" Width="25" Height="25" Padding="0">
                     <materialDesign:PackIcon Width="25" Height="25"  Kind="WindowClose"/>
                </Button>
             </DockPanel>

            <TextBlock Margin="0 40 0 0" HorizontalAlignment="Center" Text="Vous Etes Sur ?"/>

            <DockPanel Margin="40 40 40 0">
                <Button Width="80" x:Name="ok" Content="OK" HorizontalAlignment="Left" />
                <Button Width="80" x:Name="Cancel" HorizontalAlignment="Right" Content="Anuller" />
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
