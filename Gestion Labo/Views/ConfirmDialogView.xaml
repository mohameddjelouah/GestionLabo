<Window x:Class="Gestion_Labo.Views.ConfirmDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gestion_Labo.Views"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="ConfirmDialogView" FontFamily="Century Gothic" Background="{x:Null}" Height="150" Width="300" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True">

    <materialDesign:ColorZone  Background="#222f3e"  VerticalAlignment="Stretch" CornerRadius="9" >
    
        <StackPanel>

            <DockPanel>
                <Button HorizontalAlignment="Right" x:Name="CloseDialog" Background="{x:Null}" BorderBrush="{x:Null}" Width="30" Height="30" Padding="0">
                     <materialDesign:PackIcon Width="30" Height="30" Foreground="White"  Kind="WindowClose"/>
                </Button>
             </DockPanel>

            <TextBlock Foreground="White" Margin="0 15 0 15" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Vous Etes Sur ?"/>

            <DockPanel Margin="30 20 30 0">
                <Button Width="100" x:Name="ok" Background="#8395a7" BorderBrush="{x:Null}" Content="Supprimer" HorizontalAlignment="Left" />
                <Button Width="100" x:Name="Cancel" Background="#8395a7" BorderBrush="{x:Null}" HorizontalAlignment="Right" Content="Anuller" />
            </DockPanel>
        </StackPanel>
    
    </materialDesign:ColorZone>
</Window>
